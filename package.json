{
  "private": true,
  "name": "040819-class",
  "homepage": "https://040819-class-kento.netlify.com",
  "scripts": {
    "deploy": "netlify deploy --prod --dir=public",
    "postdeploy": "echo 'Your site was deployed to https://040819-class-kento.netlify.com'",
    "start": "browser-sync start --server public --files public"
  },
  "devDependencies": {
    "browser-sync": "^2.26.3",
    "stylelint": "^9.10.1",
    "stylelint-config-standard": "^18.2.0"
  }
}
